name: CI Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  release:
    types:
      - published

jobs:
  ci:
    uses: ./.github/workflows/template.yaml
    with:
      python-version: '["3.12","3.13"]'
    secrets: inherit
